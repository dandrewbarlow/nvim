-- lisp.lua
-- a collection of lisp related plugins
return {

  -- easy parentheses infer
  {"gpanders/nvim-parinfer"},

  -- TODO: finding a lisp LSP that works easy with neovim has been trickier than I thought.
  -- Mason doesn't have any by default, means finding one (googling Lisp LSP somehow presents problems).
  -- I've found a couple random ones online, but some are abandoned, most everything need to be set up as a daemon, 
  -- or be run independently each time I want to use it, which is a big ask for what little I need it for. 

}
